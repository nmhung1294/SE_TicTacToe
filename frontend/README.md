## How to run UI (with nodejs already installed)
#### From inside folder ./frontend, run these commands:
- Setups:
    - ```npm install```
    - ```npm install reactstrap bootstrap```
- Run:
    - ```npm run dev```
